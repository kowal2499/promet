var PROMET = {};

(function ($) {
    'use strict'

    //----------------------------------------------------/
    // Predefined variables
    //----------------------------------------------------/

    PROMET.globals = {
        $window: $(window),
        $document: $(document),
        $body: $('body'),

        // elements
        $preloader:  $('.preloader'),
        $mapData:  $('#googleMapData'),
        $slider:  $('.slick-slider'),
        $sidebarToggle:  $('#sidebarToggle')
    }

})(jQuery);